# Test Coverage Tracker

**Project:** memory-guard

## Rules

- Test specs are written **before** implementation begins — no exceptions
- A task is **not** "complete" because the feat commit landed and tests passed. See the verification ladder below.
- Each row maps a task ID to its spec file, current test status, and the verification level achieved

## Coverage

| Task ID | Feature | Spec file | Tests written | Status | Verified by |
|---------|---------|-----------|---------------|--------|-------------|
| 001 | Resolve the `Detector` backend (memory-guard tracer + ADR) | `001-detector-backend-tracer-test-spec.md` | TC-001…TC-006 | ✅ | spec-verifier APPROVE (all 6 TCs ✓, seam/contract/stdlib invariants hold). L6: `go run . write` with `NativeDetector` (ADR-002, Go-native in-process) → PII `pii:EMAIL`-redacted, injection `allow:false`/`stored_id:null`, benign clean. L2: `go test -count=1 ./...` → `ok …memory-guard 0.294s`. Detection cost ~5.6 µs/op (budget <1 ms). TC-004 (dep-scan): no new dep, `go.mod`/`go.sum` unchanged → trivially clear. `guard.go`/`ipc.go`/`CONTRACT.md` untouched. |
| 002 | Adversarial context-poisoning test-suite for the write-gate | `002-adversarial-poisoning-suite-test-spec.md` | TC-001…TC-006 | ✅ | spec-verifier APPROVE (after one BLOCK→fix round: added hard-benign precision-regression assertion for TC-004, threshold 0.40 vs measured 0.43). L5: `go test -run TestPoisoning -count=3 ./...` → PASS, deterministic. Honest baseline: recall=0.69 (22/32 poisoning rejected), precision=0.85 (4 FP / 14 benign), fail-closed asserted per-case for both RegexDetector and NativeDetector. 10 true miss classes documented (remember:/store/standing-order framing, roleplay jailbreak, base64/URL-encoded payloads, "whenever" policy-doc framing) — the gap a stronger backend must close; F-006 promoted to active. L2: `go test -count=1 ./...` → `ok github.com/tkdtaylor/memory-guard 0.189s`. guard.go/detector.go/ipc.go/CONTRACT.md untouched. |
| 003 | Post-deletion verification across every index/copy | `003-post-deletion-residue-verification-test-spec.md` | TC-001…TC-006 | ✅ | L6 + L2. Method = ADR-003 tiered normalized substring/phrase/token-overlap, **zero new deps** (stdlib `crypto/sha256` only). `verify_delete` now returns `{confirmed, residue_detected, residue_summary?, deletion_hash}`; residue logic in `residue.go` (guard-side, NOT behind the `Detector` seam — `detector.go` untouched). TC-003 corpus (`TestResidueCorpusDetectionRate`): verbatim 5/5=100%, normalized-numeric 7/7=100%, paraphrase 0/2=0% (documented known-miss), **OVERALL 12/14=85.7% (>80% bar ✓), precision 100% (0 FP / 4 clean controls)**. TC-006: `go.mod`/`go.sum` unchanged → trivially clear. L6: live `serve` over Unix socket — `verify_delete` returned `residue_detected:true` + summary on a `$5000→$5k` residue, and `residue_detected:false` (no summary) on a clean store. L2: `go test -count=1 ./...` → `ok github.com/tkdtaylor/memory-guard 0.541s`. v0 `TestVerifyDeleteConfirmsAbsence` still green (TC-005). spec-verifier APPROVE (all 6 TCs ✓; write/read path + detector seam untouched; spec/contract/diagram updated in-commit). |
| 004 | PII recognizer coverage hardening (behind the Detector seam) | `004-pii-recognizer-coverage-test-spec.md` | TC-001…TC-005 | ✅ | spec-verifier APPROVE (all 5 TCs ✓; seam invariant held — only detector.go + tests + data-model.md changed). L5: `go test -v -run TestCorpusSummary ./...` → 9 categories, recall=1.00/category, precision=1.00 (0 FP / 9 hard negatives) for both RegexDetector and NativeDetector. L2: `go test -count=1 ./...` → `ok github.com/tkdtaylor/memory-guard 0.570s`. guard.go/ipc.go/main.go/CONTRACT.md untouched. Note: spec's `1.2.3.4` hard-negative is itself a valid IP; substituted `v1.2.3` and documented in data-model.md. |
| 005 | Publish / remote follow-up (git remote + push) | `005-publish-remote-followup-test-spec.md` | TC-001…TC-003 | ✅ | **L6 — operator-observed.** Operator confirmed visibility **private** + owner `tkdtaylor` (TC-001). Remote created `gh repo create tkdtaylor/memory-guard --private` and `main` pushed; `gh repo view` → visibility PRIVATE, default branch `main`; local HEAD == remote HEAD (TC-002). Adoption package present on the remote: LICENSE, NOTICE, CONTRIBUTING.md (DCO), `.github/FUNDING.yml`, `.github/workflows/dco.yml` (TC-003). All 10 first-party `.go` files carry the SPDX header; `go build ./...` clean (TC-004). TODO.md "Publish" marked done with the URL + private visibility (TC-005). URL: https://github.com/tkdtaylor/memory-guard |
| 009 | Identity-scoped read isolation (T4 / R1) | `009-identity-scoped-read-isolation-test-spec.md` | TC-001…TC-007 | ✅ | spec-verifier APPROVE (all 7 TCs ✓; load-bearing TC-003 confirmed the STRONG content-overlap version — both A/B entries match query `"shared keyword"`, isolation holds by identity not by query-miss; `tenant-1`/`tenant-12` exact-match edge ✓; no cert parsing past `principal.go`; `go.mod` require-free; error shape + v0/poisoning/residue suites green). ADR-004 ratified Proposed→Accepted (fallback=UNBOUND-ONLY, `Attested()` ⇔ `trust_tier=="attested"`, durable per-identity index noted, `SvidVerifyingPrincipal` deferred). New `Principal` seam (`principal.go`: `Subject()`/`Attested()`, default `PreVerifiedPrincipal`) — no SPIFFE/X.509 in `guard.go`/`ipc.go`, `go.mod` require-free. `entry.identity map` → `entry.boundIdentity string` (bound at write via `boundKeyFor`, matched EXACT at read). Whole-store substring read replaced by identity-scoped linear filter over the `MemoryStore` seam. **L6** over a live `serve` socket — isolation matrix: A→A `call <EMAIL> balance secret-A` (own, PII-redacted), B→B `balance secret-B` (no secret-A), A↛B confirmed, unattested A→ `public balance note` (unbound-only), no-identity → unbound-only. **L2:** `go test ./...` → `ok github.com/tkdtaylor/memory-guard 0.653s` (TC-001…TC-006 + Principal-seam + v0 suites green). `make fitness` → `All fitness checks passed.` (seam gate intact). Spec/diagram updated in-commit. Pending spec-verifier before ✅. |
| 006 | MemoryStore seam + stdlib second adapter (T1) | `006-memorystore-seam-test-spec.md` | TC-001…TC-006 | ✅ | spec-verifier APPROVE (all 6 TCs ✓ with real assertions across BOTH stores; residue_test.go change confirmed plumbing-only — assertions intact; seam isolation + require-free `go.mod` verified). L6 (CLI/IPC) + L2. `MemoryStore` seam extracted (`store.go`: `Put`/`Get`/`Delete`/`Scan`/`All`); `MemoryGuard` holds the interface, routes all storage through it (`store.Put`/`Scan`/`Get`/`All`). Default `InMemoryStore` (the extracted v0 map); second stdlib adapter `TwoIndexStore` (primary `id→entry` map PLUS secondary `content→ids` index — ADR-005, **zero new deps, `go.mod` require-free**). TC-003/TC-005: `TestGuardBehaviorParityAcrossStores` + `TestInvariantsThroughSeam` run the same guard-behavior corpus parameterized over BOTH stores (clean/PII/poisoned write, read, delete+residue) — identical outcomes, fail-closed proven via `store.All()` empty after a poisoned write (no `Put`). TC-004: `TestNoStoreBackendLeak` — no store-backend token in `guard.go`/`ipc.go`/`main.go`/`CONTRACT.md`; swap is one-line `NewMemoryGuard(det, store)`. TC-006: `TestNoNewDependency` — `go.mod` has no `require`. L6: live `serve` over Unix socket — poisoned write `allow:false`/`stored_id:null`; `verify_delete` `confirmed:true`/`residue_detected:true` with content-snippet summary; bad JSON → `{error:{code:bad_request,…}}`. L2: `go test -count=1 ./...` → `ok github.com/tkdtaylor/memory-guard 0.647s`. v0 suites green (residue_test seedEntry/setup plumbing routed through `store.Put` — assertions unchanged; see deviation note). Verified by: L6 IPC + L2 `go test -count=1`. Pending spec-verifier before ✅. |

| 007 | Presidio-backed `Detector` (T2) | `007-presidio-detector-backend-test-spec.md` | TC-001…TC-007 | ✅ | spec-verifier APPROVE — all 7 TCs ✓; **both documented deviations judged HONEST, not gamed**: REQ-002 corpus byte-for-byte unchanged + zero Presidio `backendThresholds` entry + injection provably unchanged via verbatim `DetectInjection`→native delegation + genuine PII/NER lift asserted on its real domain; REQ-003 native default still asserts `>1ms`→Fatal (owns <1ms) while Presidio measured against a documented revised 50ms budget. `sidecar.py` imports only json/sys (no network/cred surface — corroborates base-only). Architect follow-up noted: reword REQ-002 (injection-vs-PII conflation is a spec-wording defect). Dependency-safety gate cleared by operator Docker-sandbox scan (Presidio 2.2.362 SAFE). **L5: harness (live Presidio sidecar) + L6 (live binary) — both reached.** Verified by: L5 `go test -tags presidio_live` (warm sidecar wired) + L6 `MEMGUARD_DETECTOR=presidio go run . write` + L4 N/A (no CI) + L3 `make fitness`/`make check` green + L2 `go test ./...` green. **Deployment shape = SIDECAR** (Python subprocess; Go binary stays pure-Go / `go.mod` require-free — `go list -m all` = module only); ONNX-in-process deferred behind the same seam (ADR-009). **Composite backend** (`PresidioDetector`, `detector_presidio.go`): native structured PII FIRST (preserves every corpus floor) + Presidio NER overlay (PERSON/LOCATION/NRP/… — the recall lift); injection delegated to native UNCHANGED. Config-driven via `MEMGUARD_DETECTOR` (`NewDetectorFromConfig`, `detector_config.go`); regex/native still selectable; unknown name → fail-closed. **Honest findings (ADR-009):** (1) REQ-002's "recall>0.69 on adversarialCorpus" is an INJECTION number a PII engine cannot lift — measured injection recall UNCHANGED native=0.6875=presidio (22/32); the lift is on PII/NER (native 0/3 PERSON vs presidio 3/3 + LOCATION). The `adversarialCorpus` is NOT modified; NO Presidio entry added to `backendThresholds`. Surfaced as a spec issue, not gamed. (2) REQ-003's <1ms is microsecond-scale for the in-process native default; the sidecar is ~ms/op — measured **~3.93 ms/op** warm (cold-start model load ~2-3s paid once at Start, excluded), REVISED rich-backend budget 50ms recorded in ADR-009; native default keeps <1ms. **dep-scan** (`presidio-analyzer`/`anonymizer` 2.2.362 pinned, base-only): all security checks pass (install_scripts/obfuscation/vulnerability/typosquatting/maintainer_change), informational `pypi_provenance` WARN accepted per operator Docker-sandbox clearance; `code-scanner` unavailable in-env → operator scan stands. **Seam clean:** `make fitness` green (F-004 seam gate — no Presidio type in guard/ipc/main/CONTRACT); `TestNoPresidioLeakPastSeam` in default pass. **L2:** `go test ./...` → `ok github.com/tkdtaylor/memory-guard`. Spec (configuration/interfaces/architecture/behaviors) + diagram updated in-commit. **Pending spec-verifier before ✅.** |

## Backlog — true-v1 increment (roadmap "Toward a true v1")

Scaffolded 2026-06-24; specs written, **not yet started** (status ❌). These flip to 🟡/✅ only after a
task branch implements + verifies them. The gating task for the v1 label is **011** (contract tracer) —
it depends on **006**. Note: **009** was re-scoped 2026-06-24 from ⚠️-blocked to ❌-startable — its
verifiable-identity issuer already exists in agent-mesh; only the identity-propagation contract decision
remains (vault dropped as a dependency).

| Task ID | Feature (roadmap) | Spec file | Tests written | Status | Depends on |
|---------|-------------------|-----------|---------------|--------|------------|
| 006 | MemoryStore seam + one real adapter (T1) | `006-memorystore-seam-test-spec.md` | TC-001…TC-006 | ✅ (see Coverage table) | — (foundational; unblocks 008, 011) |
| 007 | Presidio-backed `Detector` (T2) | `007-presidio-detector-backend-test-spec.md` | TC-001…TC-007 | ✅ (see Coverage table) | ADR-009 written + dep-scan cleared (informational provenance WARN accepted) |
| 008 | Residue proof across every index/copy (T3) | `008-residue-across-indexes-test-spec.md` | TC-001…TC-008 | ✅ | spec-verifier APPROVE (all 8 TCs ✓ with real threshold/Fatalf assertions; seam invariant intact; task-003 tests byte-for-byte unchanged). New backend-agnostic seam method `AllByIndex() map[string][]entry` (ADR-006); `verify_delete` scans EVERY index, `residue_summary` names it. **L5**: `TestMultiIndexResidueRate` → 10/10=100% (primary 4/4, secondary 6/6), 0 FP / 3 controls (≥80% bar ✓); `TestParaphraseSubCorpusImprovedSeparately` → 3/4 number-word class vs 0/2 task-003 baseline, measured separately, precision held, free-form-synonym miss recorded honestly. Partial L6: real in-process `VerifyDelete` over a multi-index store → `residue_detected:true`, summary names index `"recency-cache"` (a primary-only scan would miss it). TC-005 backward-compat: single-index reduces exactly to task-003 scan, `residue_test.go`/`guard_test.go` empty diff vs main. Stdlib-only paraphrase (number-word canonicalization), `go.mod` require-free. `make fitness` green (seam gate). L2: `go test ./...` → `ok github.com/tkdtaylor/memory-guard`. | **006** |
| 009 | Identity-scoped read isolation (T4 / R1) | `009-identity-scoped-read-isolation-test-spec.md` | TC-001…TC-007 | ✅ (see Coverage table) | 006 + identity-propagation contract — both satisfied; ADR-004 ratified |
| 010 | audit-trail OCSF emission (T5 / R2) | `010-audit-trail-emission-test-spec.md` | TC-001…TC-007 | ✅ | spec-verifier APPROVE (re-verify after BLOCK→fix; all 7 TCs ✓ with regression-catching assertions, race-clean). **L5 is the ceiling — L6 (live audit-trail socket) is a soft external dep legitimately deferred per ADR-007 until the emit endpoint is confirmed; emission ships DEFAULT-OFF.** L5: fake AuditSink harness wired into guard. TC-001: event per detection class (pii_redaction, injection_rejected, residue_found, deletion_verified, benign→0 events). TC-002: OCSF envelope fields (class_uid=2001/category_uid=2/activity_id=1/severity_id typed/time/metadata.product.name/finding structured). TC-003: seam isolation — CollectingSink/FailingSink/NoOpSink/nil swapped; guard+IPC unchanged; `make fitness` seam gate green. TC-004: deletion_hash in event == deletion_hash in verdict; raw deleted content absent from event. TC-005: `alice@example.com`/`4111 1111 1111 1111`/`123-45-6789` absent from all serialized events (direct JSON scan). TC-006: verdicts byte-for-byte identical working-vs-failing sink INCLUDING `flags` (both writes) and `deletion_hash`+`residue_detected` (both guards seeded with the SAME fixed (id,content) via the store seam, so deletion_hash is genuinely comparable); write-gate `allow:false`/`stored_id:nil` on injection regardless of sink; PanicSink recovered; **slow-sink test: a 500ms SlowSink wrapped in AsyncSink returns under a 50ms hot-path bound (non-blocking dispatch — REQ-005)**. TC-007: default-OFF guard with an injected CollectingSink emits ZERO events across PII-write + injection + residue-delete (fails if default flips on); enabled→events; disabled config→0 events; nil-sink-enabled→disabled. Spec-verifier BLOCK round resolved (4 gaps fixed in `fix:` commit): GAP1 flags compared, GAP2 deletion_hash comparable + `TestDeletionHashIndependentOfSinkState`, GAP3 `AsyncSink` non-blocking dispatch (ADR-007 §6) + `TestAsyncSinkNonBlocking`, GAP4 default-off test now injects a sink and asserts zero. `go test ./...` → `ok github.com/tkdtaylor/memory-guard`. `make fitness` → `All fitness checks passed.` L6 (live audit-trail socket) deferred — pending audit-trail emit endpoint confirmation (ADR-007). |
| 011 | Contract tracer-bullet — **earns the v1 label** (T6) | `011-contract-tracer-bullet-test-spec.md` | TC-001…TC-010 | ✅ | spec-verifier APPROVE — **L5 AND L6 both genuinely reached and INDEPENDENTLY REPRODUCED by the verifier** (re-ran the built binary out-of-process via `nc -U`, transcript byte-for-byte matched ADR-008); confirmed a real out-of-process socket slice (not an in-process call) against the real `MemoryStore` seam, field+type assertions (not smoke), and the 3 live invariants end-to-end. **Caveat removal AUTHORIZED.** Follow-up applied: AGENTS.md's lingering caveat dropped in a separate `docs:` commit (was outside TC-009's enumerated list). **L6: live `serve` socket + L3 fitness + L2 unit.** Contract tracer (ADR-008): `contract_tracer_test.go` starts `serve` against a guard backed by the REAL `MemoryStore` seam (multi-index `TwoIndexStore`) + v0 `NativeDetector`; a client dials the socket and drives `validate_write → validate_read → verify_delete`, asserting each verb's response field-by-field on the JSON DECODED OFF THE SOCKET (TC-001…TC-004 via `mustKeys` exact-key + type checks) + the three live invariants (TC-005 poison rejected & never persisted via follow-up read; TC-006 PII `<EMAIL>`/`<US_SSN>`-redacted over the wire, raw absent; TC-007 delete proven absent via follow-up read + idempotent re-delete). TC-008 `TestTracerRefinementRecordedInADR` — ADR-008 records the shapes verbatim + "validated unchanged" (NO refinement forced). TC-009 `TestTracerCaveatRemoved` — "not yet tracer-validated" caveat removed from CONTRACT.md/SPEC.md/README.md/roadmap.md (in-commit). TC-010 `TestTracerReadinessRealStoreSeam` — guard runs against the real store seam, NOT the bare map; detector dimension validated against v0 NativeDetector (Presidio/007 not merged), real-backend re-validation a noted follow-up. **L6:** out-of-process `go run . serve --socket /tmp/mg-l6.sock` driven by a separate client — verbatim transcript: `validate_write -> {"allow":true,"flags":["pii:EMAIL","pii:US_SSN"],"stored_id":"mem-299868a23f02"}`; `validate_read -> {"allow":true,"content_redacted":"contact <EMAIL>, SSN <US_SSN>, meeting in room 4","flags":[]}`; `verify_delete -> {"confirmed":true,"deletion_hash":"c6bb86ff…","residue_detected":false}`; poison `{"allow":false,"flags":["injection_suspected"],"stored_id":null}`; post-delete read `content_redacted:""`. **L2:** `go test ./...` → `ok github.com/tkdtaylor/memory-guard`. **L3:** `make fitness` → `All fitness checks passed.` Pending spec-verifier before ✅. | **006** (ideally 007) |
| 013 | Injection-recall lift — stronger `DetectInjection` heuristic | `013-injection-recall-lift-test-spec.md` | TC-001…TC-007 | ❌ | — (detector-internal; startable now. Resolves ADR-009 Finding 1's dangling concern — lift INJECTION recall > 0.6875 on the UNMODIFIED `adversarialCorpus` via a raised `backendThresholds` entry, precision held ≥ 0.85, behind the unchanged `Detector` seam, stdlib-only. Security-sensitive: write-gate change → security-auditor pass before ship.) **SUPERSEDED by 014** — the regex char-distance framing-anchor method was rejected after failing adversarial audit 3× (SEC-001/006/007); see ADR-010. Goal re-homed to 014. |
| 014 | Injection-recall lift — phased (Phase A no-collision recoveries shipped; Phase B deferred) | `014-injection-recall-lift-phased-test-spec.md` | TC-001…TC-004, TC-008…TC-010 (Phase A); TC-005…TC-007 deferred | ✅ (Phase A) / deferred (Phase B) | **Phase A ✅ — spec-verifier APPROVE + mandatory security-auditor APPROVE ([ADR-011](../../architecture/decisions/011-cap-injection-recall-at-phase-a.md)).** Phase A recovered the 4 no-collision miss-classes (`[INJECT:]` bracket prefix, AI-object-anchored jailbreak, base64/URL decode-then-rescan with SEC-004 bounded-decode caps) over the **byte-for-byte-unchanged** `adversarialCorpus`. **Measured recall 0.8125 (26/32)** — strictly > the 0.6875 baseline — **precision 0.8667**, both `RegexDetector` and `NativeDetector`; raised `backendThresholds` (recall 0.70 / precision 0.65 floors) + F-006 (recall ≥ 0.80, precision ≥ 0.85) updated in place. Security-auditor BLOCK→fix round: SEC-A-001 (Critical, bare `developer mode` over-rejection) + SEC-A-002 (DAN-matches-name) found and FIXED (dropped — caught zero corpus cases; product-settings + person-note benign fixtures added so the class can't pass blind again), re-audit APPROVE. **L6**: live `go run . write` — base64/`[INJECT:]`/`unrestricted AI` → `allow:false`/`stored_id:null`; benign `[INJECT]` macro / `unrestricted trial` / `developer mode` settings / `Dan` person-note → `allow:true`. **L3** `make fitness` → `All fitness checks passed.` **L2** `go test -count=1 ./...` → `ok`, deterministic ×3. Seam (`guard.go`/`ipc.go`/`CONTRACT.md`) byte-for-byte unchanged; `go.mod` require-free; stdlib-only (`encoding/base64`/`net/url`); fail-closed. **Phase B (TC-005…007) DEFERRED indefinitely (ADR-011):** the token-level negation-scope analyzer reached recall 1.00 with a green suite but failed the adversarial security-auditor gate **4 consecutive rounds** — each a fresh ordinary-English fail-open (SEC-B-001…008: inflections, cease-synonyms, particle/phrasal halt verbs, pre-posed cessation, comma-parentheticals). Per ADR-010's *sound-gate-beats-fail-open* principle, capped at Phase A; the unsafe Phase-B branch commits (`2289bd5`→`1438cf1`→`90be3a1`) were reset off the task branch and never merged. A future Phase B needs a grammatical-parse / dependency-backed method behind the same seam, not more stdlib token-rules — not an actionable backlog item. |
| 012 | Fitness-function runner wired as a gate (T7) | `012-fitness-function-runner-test-spec.md` | TC-001…TC-008 | ✅ | spec-verifier APPROVE (7/8 TCs ✓ with live breach evidence; 1 non-blocking ⚠ — no `warn`-severity rule exists to exercise the warn-path edge, all wired rules are `block`). **L3 is this task's ceiling — a dev-gate, no runtime/server surface**; the gate's own behavior was operator-observed: `make fitness`/`make check` → `All fitness checks passed.` (exit 0) on the clean tree, and each rule's synthetic-breach fixture exits non-zero with a `measured X vs threshold Y` delta — `fitness-latency` `measured 2.13ms vs threshold 1ms`; `fitness-recall-precision` `[zeroRecallDetector] recall measured 0.00 vs threshold 0.68`; `fitness-seam` `guard.go:1: backend token "PresidioClient" leaked past seam`. F-004/F-006/F-007 flipped `proposed`→`active` in-commit. Floors honest (recall 0.68 / precision 0.84 — ≤0.75pp under documented baseline). TC-008: `go.mod` require-free. L2: `go test ./...` → `ok github.com/tkdtaylor/memory-guard`. |
| 015 | Real MemoryStore adapter (file-backed, persistent) (T1) | `015-real-memorystore-adapter-test-spec.md` | TC-001…TC-011 | 🟡 | feat commit landed. New persistent `FileStore` (`store_file.go`, ADR-012): JSONL snapshot rewritten atomically (temp+fsync+rename, mode 0600), read-through-disk, byte-level delete proof; `NewStoreFromConfig` factory (`store_config.go`) + `main.go` `MEMGUARD_STORE`/`MEMGUARD_STORE_PATH` wiring (exit 2 fail-closed). Default stays `memory`. Seam intact: `FileStore`/`fileRecord` added to `seamBannedStoreTokens` + `TestNoStoreBackendLeak`; `go.mod` require-free. **L2:** `go test ./...` → `ok`. **L3:** `make fitness` → `All fitness checks passed.`; `MEMGUARD_FITNESS_SEAM_BREACH=FileStore make fitness-seam` → non-zero. **L6 (pending spec-verifier before ✅):** live `serve` w/ `MEMGUARD_STORE=file` — startup names `store: file`; `grep -c veloheliotrope` = 2 between socket write and delete, `0` after all `verify_delete`s (byte-level proof over the persistent store). | **006** |

## Status key

| Symbol | Meaning |
|--------|---------|
| ✅ | **Verified** — validation harness exercised the live runtime path, or operator observed the targeted behaviour |
| 🟡 | **Code merged** — feat-commit landed, unit tests + fitness + CI green, but runtime/live behaviour not yet observed |
| ⏳ | In progress |
| ❌ | Not started |
| ⚠️ | Blocked |

## Verification ladder

A task earns 🟡 at levels 1–4 and ✅ only at level 5 or 6. The `Verified by` column records which level the row reached.

| Level | Evidence | Status this earns |
|-------|----------|-------------------|
| 1 | Code merged | 🟡 |
| 2 | Unit tests pass (paste verbatim final line of `go test ./...`) | 🟡 |
| 3 | `make fitness` passes (verbatim closing line) | 🟡 |
| 4 | CI passes (`gh run watch <id> --exit-status` → success) | 🟡 |
| 5 | **Validation harness** exercises the live runtime path end-to-end — paste the command and the final assertion line | ✅ |
| 6 | **Operator-observed** — operator (or executor via `go run .` / a live `serve`) saw the targeted behaviour in stdout / logs | ✅ |

If the task targets runtime-observable behaviour (CLI args, server endpoints, file outputs, side effects), level 5 or 6 is **required** before flipping to ✅. If the task only adds an internal helper covered by unit tests, level 2 may be sufficient — but in that case the row's `Verified by` should explicitly say "unit-test-only; no runtime surface" so future readers don't mistake silence for verification.

## Rule

**The task-executor commits at 🟡 by default.** Only the main session (after spec-verifier APPROVE + the appropriate level-5/6 evidence) updates the row to ✅, in a separate commit titled `verify: confirm task NNN — <level-5/6 evidence>`. This keeps the verification step visible in git history and prevents "merged ≠ done" drift.
</content>
