# SPDX-License-Identifier: Apache-2.0
#
# Pinned, BASE-ONLY Presidio dependency set for the memory-guard sidecar (task 007 / ADR-009).
#
# Dependency-safety gate: CLEARED — Presidio scanned in a Docker sandbox (operator, 2026-06-24):
# presidio-analyzer / presidio-anonymizer 2.2.362 are SAFE — no install hooks, no obfuscation,
# no bundled binaries, no exfil; dep-scan all-pass except one informational pypi_provenance warn.
# The credential-reading code lives ONLY in optional extras (azure / openai / langextract /
# transformers / gliner / stanza) that a BASE install never pulls — so we install the base
# packages ONLY and pin them exactly. The sidecar needs NO outbound network at runtime.
#
# To provision (offline-capable once cached):
#   python3 -m pip install -r presidio/requirements.txt
#   python3 -m spacy download en_core_web_lg   # pinned to 3.8.0 below
#
# DO NOT add azure/openai/transformers/gliner/stanza extras — they reintroduce the
# credential-reading surface the scan flagged as gated. See ADR-009 + docs/spec/configuration.md.

presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362

# spaCy NER engine + the English model (pinned; en_core_web_lg is installed via
# `python -m spacy download en_core_web_lg` which resolves to en-core-web-lg==3.8.0).
spacy==3.8.14
# en_core_web_lg==3.8.0   # installed out-of-band via `python -m spacy download` (model wheel)
